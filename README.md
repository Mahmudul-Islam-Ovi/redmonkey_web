# RedMonkey — Animator Portfolio (Next.js App Router + TypeScript + Tailwind + shadcn/ui)

This project uses Next.js App Router (the `/app` directory), TypeScript, Tailwind CSS, and references shadcn/ui components.

Setup:
1. Install dependencies:
   npm install
   # You may need to install shadcn/ui and Radix UI packages per shadcn documentation.

2. Run dev:
   npm run dev

Notes:
- Replace images in public/images/
- shadcn/ui components are referenced in components/ but you must install additional packages (see shadcn docs).
