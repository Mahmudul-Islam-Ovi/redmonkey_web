/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};
module.exports = nextConfig;


// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   reactStrictMode: true,
//   output: 'export',               // REQUIRED for static export (App Router)
//   images: {
//     unoptimized: true,            // REQUIRED if using next/image
//   },
// };

// module.exports = nextConfig;
